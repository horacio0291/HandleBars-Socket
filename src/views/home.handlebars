<div class="container d-flex flex-wrap justify-content-between align-items-center justify-content-center">

    {{#each products}}
    <div class="card" style="width: 18rem;">
        <img src={{thumbnail}} class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{title}}</h5>
            <p class="card-text">{{descripction}}</p>
        </div>
    </div>
    {{/each}}
</div>